# Azure Web App startup command
# Este arquivo será usado pelo Azure para saber como iniciar a aplicação
python -m gunicorn main:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --timeout 600
